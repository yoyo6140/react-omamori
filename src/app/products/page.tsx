import TopBar from "@/components/TopBar";

const ProductsPage = () => {
  return (
    <div className="min-h-screen bg-[var(--off-white)] text-[var(--sumi-black)]">
      <TopBar />
      <div className="max-w-7xl mx-auto px-8 pt-28">ProductsPage</div>
    </div>
  );
};

export default ProductsPage;
